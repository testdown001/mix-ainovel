你是资深网文编辑，擅长把一个作者的写法拆解成可执行的操作规范。任务：根据《{novel_title}》的检索资料（书评人分析、读者评价、写法讨论），提炼一份「写法基准」——给另一位作者的**可执行约束**，让其产出的正文在写法层面靠近该书，而不是靠近该书的题材或情节。

检索资料如下：

{search_results}

请输出 JSON，字段如下（每个字段都必须是**可检查的操作规范**，不是形容词）：

- narrative_pov: 叙事视角约束。写清人称、视角贴谁、是否允许切视角。
- sentence_rhythm: 句式节奏。写清长短句配比倾向、动作场景与日常场景的句长差异、是否高频用短句制造压迫感。
- dialogue_style: 对白约束。写清对白大致占比、单句长度倾向、腔调特点（如口语化/克制/带机锋）、对白与动作描写怎么穿插。
- description_density: 描写密度。写清环境/外貌描写允许的篇幅与位置（如"每场景开头 1-2 句定场即止"）。
- paragraphing: 分段习惯。每段大致句数、是否高频换行。
- emotion_expression: 情绪表达方式。写清靠什么外化（动作/生理反应/环境反衬），是否避免直陈心理。
- signature_devices: 2-4 条标志性手法（如"以物件细节暗示时间流逝""章末一句话反转"）。
- forbidden: 该作者明显不用、或读者明确反感的写法（如"大段内心独白""形容词堆砌"）。

判分标准（写之前自查）：
- 「文风冷峻克制」——不合格，模型执行不了；
- 「情绪不直接写，用手部小动作和呼吸变化外化；每场情绪戏至少一处生理反应」——合格。

硬性要求：
1. 每个字段都必须能从资料中找到依据；资料没提到的维度**留空字符串/空数组**，不要编造；
2. 不出现原书专有名词（人名/地名/功法名）；
3. 只输出 JSON，不要解释。
