# Arboris-Novel 产品与工程路线图

> 状态：M0、M1、M2、M3 已完成（当前进入发布加固与 M4 启动前验收）
> 更新日期：2026-08-22
> 适用范围：面向单人长篇中文网络小说作者的创作主链路
> 核心原则：运行时代码与真实作者体验是唯一事实来源；本文档指导技术演进与产品优先级。

---

## 1. 北极星与产品定位

Arboris-Novel 的定位是**“专业级长篇网文创作副驾与全书设定中枢”**，而非“替代作者写完一本书的黑盒枪手”。系统确保作者在从灵感构思到成书定稿的全过程中拥有：

1. **绝对安全感（Reliability）**：正文零丢稿风险，网络波动/中断状态真实可信，任意改动可溯源、可对比、非破坏性恢复。
2. **创作主导权（Author Agency）**：正文由作者定稿；AI 以“秒级初稿粗胚 + 选区手术刀局部精修”双轨协作，非作者确认不覆盖正文。
3. **长篇一致性（Consistency & State Tracking）**：世界观、人物状态机、境界实力、时间线与伏笔网络具备确定性的文本锚点证据链。
4. **工业交付力（Deliverability）**：结构化多卷章管理，支持行业标准格式导出与平台敏感词/违规预检。

首发用户聚焦于**单人长篇中文网文创作者（连载/签约作者）**。

### 核心创作闭环

```text
灵感蒸馏/压力推演 ➔ 蓝图与章纲落盘 ➔ 设定/宪法播种 ➔ 写作台双轨创作(预生成初稿+选区精修) ➔ 证据级跨章诊断 ➔ 活大纲双向演化 ➔ 标准导出
```

---

## 2. 近期演进复盘与现状基线（基于最近 50+ 次迭代）

系统近期已完成多项重构与关键资产沉淀，成为本路线图的演进基石：

| 领域 | 近期已落地资产 (已完成) | 现存不足与演进方向 |
| :--- | :--- | :--- |
| **灵感与立项** | • 对话蒸馏结构化立项书 (`concept_dossier`)<br>• 白金主编 50/100/300 章冲突供血与毒点压力推演<br>• 七维商业蓝图审稿门<br>• 25 章/批章级规划落盘<br>• `NovelConstitution` 宪法自动播种 | 缺乏正文脱轨时向立项书/大纲的反向回写与重平衡机制。 |
| **章节生成** | • 选版后异步预生成下一章使命 (`mission_pregen`)，写作前等待从 120s 降至秒级<br>• 支持 Fast / Standard / Literary 场景级生成 | 后处理链路存在 6~10 轮串行 LLM 改写的传话筒效应；场景生成存在“近视接续”盲区。 |
| **写作台交互** | • 正文优先界面：大纲/规划收拢为“本章要点”，正文顶到第一屏<br>• 选区手术刀：划选文本定向扩写/润色/去AI味，Diff 对照与原子替换<br>• 重起草降权为辅助操作 | 需补齐输入防抖本地暂存 + 关键里程碑快照的分层不可变版本存储。 |
| **记忆与参考** | • 参考小说情境桥段库，清洗思维过程与任务复述<br>• Qdrant 向量检索 + BM25 混合检索 | 纯向量检索无法感知确切“世界状态（角色伤势/当前位置/背包道具）”。 |
| **系统鲁棒性** | • 断线状态机修正（拒绝幽灵生成中）<br>• 进度事件统一与超时降级通知<br>• 评分通道 (grader) 独立配置与用量体检 | 异步知识抽取需增加增量机制，防止多章全量重跑导致 API 成本失控。 |

---

## 3. 核心设计原则

1. **先可靠，后智能**：P0 级保障“保存、恢复、冲突检测、不可变快照”绝对稳定前，不堆砌花哨的画布与评分动效。
2. **双轨协作（Dual-Track Writing）**：保留“基于使命的秒级整章起草”用于破除白纸焦虑；强化“选区手术刀”用于精雕细琢。
3. **单阶段引导精修（Single-Pass Guided Polish）**：逐步将多轮串行后处理收敛为“1次结构化诊断 + 1次针对性精修”，彻底杜绝过度平滑与文风丢失。
4. **事实状态机优先（State Machine First）**：长篇连续性依赖结构化状态（角色位置/境界/持有物/知情状态）+ 文本范围锚点证据，而非仅靠模糊向量相似度。
5. **双向演化（Bidirectional Evolution）**：正文是最高事实；当正文发展偏离蓝图时，大纲与未来规划支持一键增量逆向对齐。
6. **分层版本存储（Tiered Immutable Snapshots）**：前端防抖本地暂存，服务端仅在主动保存、AI 采纳、离页等关键节点生成不可变修订版本，防止数据库膨胀。

---

## 4. 优先级与阶段规划（Milestones M0 ~ M6）

```
[M0 契约与ADR] ➔ [M1 领域模型收口] ➔ [M2 可靠写作底座] ➔ [M3 版本/恢复/导出]
                                                               │
                                  ┌────────────────────────────┴───────────────────────────┐
                                  ▼                                                         ▼
                       [M4 写作台双轨协作与单段精修]                               [M5 状态机知识层与双向大纲演进]
                                  │                                                         │
                                  └────────────────────────────┬────────────────────────────┘
                                                               ▼
                                                  [M6 高级创作空间与视图增强]
```

| 阶段 | 状态 | 优先级 | 核心目标 | 主要交付物 | 预计周期 |
| :--- | :---: | :---: | :--- | :--- | :---: |
| **M0** | 已完成 | P0 | 产品与技术契约 | • ADR 架构决策记录<br>• 统一版本语义与错误码字典<br>• 核心埋点与成本监控标准 | 3–5 天 |
| **M1** | 已完成 | P0 | 领域模型与状态收口 | • 独立 `Volume` 卷实体与章节稳定排序<br>• 整合 `concept_dossier` 与蓝图<br>• 显式“世界状态切片”结构定义 | 1–2 周 |
| **M2** | 已完成 | P0 | 可靠写作主链路 | • 输入防抖 + 本地离线草稿保护<br>• 乐观锁防多端覆盖与冲突合并提示<br>• 断网/异常恢复重试 | 2 周 |
| **M3** | 已完成 | P0 | 不可变版本与多格式导出 | • 不可变版本历史（恢复操作生成新版本）<br>• 两版文本可视 Diff<br>• Markdown / DOCX / TXT 结构化导出与词表预检 | 1–2 周 |
| **M4** | 待启动 | P1 | 双轨 AI 协作与后处理收敛 | • 选区手术刀原子采纳与回滚<br>• 预生成使命支撑的高质量初稿起草<br>• **后处理重构为单阶段精修**（告别 6-10 轮洗稿） | 2–3 周 |
| **M5** | 待启动 | P1 | 状态机知识层与双向大纲演化 | • 增量异步知识抽取（带章节/文本范围锚点）<br>• 客观冲突诊断（姓名/境界/时间线/生死状态）<br>• **正文脱轨时一键逆向重平衡大纲 (Re-Outline)** | 3–4 周 |
| **M6** | 待启动 | P2 | 视图增强与创作者生态 | • 人物关系图谱/故事线视图（纯基于领域模型视图）<br>• 创作目标与长篇节奏大盘<br>• 投稿平台格式预检与分卷打包 | 按需排期 |

---

## 5. 各阶段详细实施要点

### 5.1 M0：产品契约与架构决策（P0）
- 明确非目标：不搞全自动代笔写书、不搞复杂无边界的独立画布数据层、不搞未经作者确认的后台改文。
- 制定关键 ADR：
  - **ADR-01**：章节版本的不可变性与分层存储策略（Local Draft vs DB Snapshot）。
  - **ADR-02**：后处理流水线的收敛标准（单次诊断 + 单次重写）。
  - **ADR-03**：知识抽取触发策略（定稿触发 vs 增量差异触发）。

### 5.2 M1：领域模型与世界状态收口（P0）
- **卷（Volume）实体化**：从 JSON 迁移为一等数据库实体，具备独立字数统计、排序索引、分卷大纲与导出边界。
- **世界状态切片（World State Snapshot）落地**：
  - 为章节引入轻量结构化状态字段（包含关键角色当前境界、伤势、物理位置、阵营倾向、秘密知情状态）。
  - 章节定稿时继承并递增生成下一章的世界状态初值，替代脆弱的纯语义向量检索。
- **数据结构兼容**：深度对齐已有的 `concept_dossier`、`NovelConstitution` 与 `ChapterOutline.metadata_["pregen_mission"]`。

#### M0/M1 完成记录（2026-08-21）

- M0：已落地 ADR-001～003、稳定错误码词典与低耦合结构化观测事件。
- M1：已迁移 `Volume` 一等实体，章节保有稳定 `sort_key` 与卷归属；旧 `blueprint.volumes` 保留为兼容投影。
- M1：已落地带版本、来源与文本范围证据的不可变世界状态切片，并提供下一章的继承种子接口。
- 边界：自动抽取、冲突诊断及正文反向大纲演进仍属于 M5；M1 不会未经作者确认覆盖世界状态或正文。

### 5.3 M2：可靠写作主链路（P0）
- **输入防抖与离页保护**：前端 IndexedDB 实时缓存未保存字符；网络异常时显示明确黄色警示，阻止意外关页。
- **乐观锁与并发安全**：章节提交携带 `revision_id` 与内容哈希，拒绝静默覆盖。出现冲突时提供“保留本地 / 采用远端 / 另存为新分支”三路决策。

#### M2 完成记录（2026-08-21）

- 编辑器以 500ms 防抖把未保存正文写入 IndexedDB；关闭、隐藏页面及离线状态均会尽力刷写本机草稿。
- 新增章节安全保存协议：`revision_id + content_hash` 条件写入、同内容网络重试幂等、冲突返回稳定 `VERSION_CONFLICT` 错误码且不在错误体中泄露正文。
- 每次手工保存、选区采纳或优化采纳都会创建新的 `ChapterVersion` 并推进章节修订号；旧 `edit`/`edit-fast` URL 仅接受携带版本基线的兼容请求。
- 冲突 UI 已提供保留本地、采用远端和另存为分支三种作者可控的处理方式；联网恢复后会自动重试尚未提交的本地内容。

### 5.4 M3：版本、恢复与专业交付（P0）
- **不可变修订历史**：已为 `ChapterVersion` 写入来源、父版本、内容哈希、变更说明与操作者；恢复操作一律追加 `history_restore` 新快照，历史版本永久只读。
- **差异比较器（Diff Viewer）**：已在写作台提供按需加载的双栏红绿 Diff；中文按字、英文按词比较，并显示手写、选区改写、整章起草、优化与历史恢复来源。
- **专业导出引擎**：已支持 Markdown、TXT、DOCX 的卷/章/正文缩进结构化导出；导出仅在当前 HTTP 响应内存中生成并流式下发，不在服务器保存副本。投稿敏感词预检可下载独立报告，且不会阻断导出。

### 5.5 M4：章节内双轨 AI 协作与后处理精炼（P1）
- **选区手术刀（P0）**：
  - 划选正文呼出微型工具条（扩写、润色、精简、强化冲突、去 AI 味、古典化）。
  - 局部建议以差异卡片呈现，点击“采纳”原子合入并生成修订节点，点击“放弃”立即复原。
- **整章起草优化（P0）**：
  - 复用 `mission_pregen` 预生成的章节使命，提供秒级初稿生成，作为作者的起步粗胚。
- **后处理流水线革命（P1）**：
  - **废弃 6~10 轮串行改写**，重构为 **`ProseSculptor` 二段式精修**：
    1. 步一（诊断）：一次性扫描行文违规、设定冲突与 AI 腔调。
    2. 步二（定向重写）：仅针对标记段落做单次综合性润色与护栏修正。
  - 彻底杜绝语义漂移、文风磨灭与字数反复缩水。

### 5.6 M5：知识层事实诊断与双向大纲演化（P1）
- **异步增量知识索引**：
  - 章节被标记为“完成/定稿”时触发，按段落级差异异步提取实体出场、伏笔引用与事件线索。
  - 索引记录附带 `chapter_number`、`version_id`、`char_start`、`char_end` 与置信度。
- **高确定性事实诊断**：
  - 优先阻断客观硬伤：人物死亡后登场、称谓性格冲突、设定规则违背、长期伏笔超期未提。
  - 诊断提示均附带原文证据跳转链接，支持作者标记“已处理 / 故意为之 / 忽略”。
- **双向大纲重平衡（Re-Outline）**：
  - 当检测到正文实际情节与原定大纲产生实质性偏离时，触发“逆向大纲对齐建议”：
  - 作者可一键选择“基于第 N 章新正文，重新推演后续 20 章大纲与伏笔网络”。

### 5.7 M6：高级创作空间与视觉增强（P2）
- **纯视图化关系图谱**：人物势力网、伏笔生命周期甘特图、情节起伏曲线。所有图谱节点均直接映射底层数据库实体，不支持脱离实体的悬空编辑。
- **创作进度大盘**：连载字数统计、分卷完成率、日均产出与写作状态热力图。

---

## 6. 关键风险与应对策略

| 核心风险 | 潜在后果 | 架构控制方案 |
| :--- | :--- | :--- |
| **断网/崩溃丢稿** | 摧毁作者对平台的信任底线 | 前端 IndexedDB 实时防抖本地存 + 服务端乐观锁 + 离页强拦截。 |
| **后处理破坏文风** | 生成的小说平庸套路，丢失作者原味 | 废弃多轮洗稿链，严格推行“选区手术刀”与“单阶段诊断式定向精修”。 |
| **异步知识抽取成本失控** | 百万字全书频繁重跑导致 API 账单爆炸 | 仅在章节“定稿”时触发增量 Diff 抽取；长篇使用低成本轻量模型前置过滤。 |
| **大纲正文脱节** | 正文自由发挥后，后续 AI 辅助全盘跑偏 | 引入 M5“正文逆向演进大纲”机制，使蓝图具备动态自适应能力。 |
| **版本数据量膨胀** | 频繁自动保存导致数据库产生海量碎片记录 | 分层设计：输入防抖仅存浏览器本地，显式保存/AI采纳才上链成为不可变版本。 |

---

## 7. 执行验收门禁（Gateways）

- **M0 ➔ M1**：产品非目标、ADR 决策文档、版本分层规范评审通过。
- **M1 ➔ M2**：数据库迁移脚本幂等验证完成，`Volume` 与状态机模型通过一致性测试。
- **M2 ➔ M3**：断网重连、刷新重载、并发多端冲突合并 100% 达成无损测试。
- **M3 ➔ M4**：版本不可变回滚与 DOCX/Markdown 结构化导出验收通过。
- **M4 ➔ M5**：选区手术刀采纳体验流畅，单阶段精修使后处理耗时降低 60% 以上且文风保持率达标。
- **M5 ➔ M6**：高确定性事实诊断误报率低于 15%，逆向大纲重平衡测试通过。
